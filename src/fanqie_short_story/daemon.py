"""Daemon layer: macOS launchd integration (v0.3.0 stub).

Full implementation lives in Chunk 3 of the v0.3.0 plan. This stub exists
only so the `fanqie-story-run` console script entry point resolves at
`pip install -e .` time.
"""
from __future__ import annotations


def run_with_notification() -> int:
    """Placeholder — Chunk 3 replaces this body."""
    raise NotImplementedError(
        "run_with_notification is implemented in Chunk 3 (Task 12)"
    )
